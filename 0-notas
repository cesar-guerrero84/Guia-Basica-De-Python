puedes usar python si lo tienes instalado en la consola de tu pc como cmd por ejemplo tecleando python

en python para escribir texto es igual a js con "" puedes usarlo solo con comillas o con la funcion print("el texto aqui") que seria como el console.log en js

python se ejecuta en la terminal con python y el archivo.py donde lo tengas guardado esto es por que python es un lenguaje interpretado recuerda que debes estar en la carpeta donde se encuentra el archivo.py y escribir python archivo.py

con python -h obtienes ayuda de como interactuar con el interprete de py

con # dejas comentarios

python es sentiveCase osea que es muy sensible a las mayusculas y minuculas por lo que debes poner mucho cuidado a como estan escritos ciertas funciones y tambien a como desginas tus palabras en variables por ejemplo ya que una sola letra en mayusculas o minuculas crea una var nueva

dir(aquiPonesLoQueQuieresExaminar) te enseña las formas que puedes alterar un dato especifico (propiedades / metodos en funciones)